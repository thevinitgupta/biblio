// // src/services/logger.ts

// const getLogger = (isClientSide : boolean) => isClientSide
//   ? require('./logger.client').default
//   : require('./logger.server').default;

// export default getLogger;
